{cover_enabled, true}.

{plugin_dir, "src/plugin"}.
{plugins, [perforator_rebar_plugin]}.

{erl_opts, [{i, "include"}, {parse_transform, sheriff}]}.
{eunit_opts, [verbose]}.

{cover_enabled, true}.

{deps, [
    {sheriff, ".*", {git, "https://github.com/extend/sheriff", "342015e610"}}
]}.
